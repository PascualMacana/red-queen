[package]
name = "reinante"
version = "0.1.0"
edition = "2021"
description = "A constructor that rewrites itself while the evaluator keeps moving."
license = "MIT"
publish = false
repository = "https://github.com/PascualMacana/reinante"

[[bin]]
name = "reinante"
path = "src/main.rs"
